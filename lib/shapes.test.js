const { createCircle, createTriangle, createSquare } = require('./shape');

test('Create Circle SVG Element', () => {
});

test('Create Triangle SVG Element', () => {
});


test('Create Square SVG Element', () => {
});